# Dependencies
node_modules/

# Capacitor native project (generated by `npx cap add android`).
# Many teams DO commit android/ — if you customize native code beyond
# the documented manifest/gradle edits, remove this line and commit it.
android/

# Build outputs
dist/
*.aab
*.apk

# Signing secrets — NEVER commit these
*.keystore
*.jks
keystore.properties

# OS / editor noise
.DS_Store
Thumbs.db
